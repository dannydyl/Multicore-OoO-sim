# Benchmarks

A full benchmark write-up is on deck — Phase B in the
[docs roadmap](../study/index.md). It will cover:

- Which traces ship with the repo (synthetic + project-derived + SPEC2017 mixes).
- IPC numbers from the [config sweep](../report/09-config-sweep.md) (~30 configs across cores / protocols / cache geometry / DRAM latency / OoO width / predictor).
- The [heterogeneous real-trace mix results](../report/17-heterogeneous-real-mix-results.md).
- How to reproduce each number end-to-end.

Until then, the underlying reports already have the data — start with the
[config sweep](../report/09-config-sweep.md) and
[baseline characterization](../report/15-baseline-characterization.md).
